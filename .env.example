# ============================================
# Bedrock Knowledge Base MCP Server 配置
# ============================================

# 必需配置
# --------------------
# Knowledge Base ID（必需）
KNOWLEDGE_BASE_ID=your-knowledge-base-id-here

# AWS 区域（必需）
AWS_REGION=us-east-1

# 可选配置
# --------------------
# Knowledge Base 过滤标签
KB_INCLUSION_TAG_KEY=mcp-multirag-kb
KB_TAG_VALUE=true

# Bedrock 重排序功能
BEDROCK_KB_RERANKING_ENABLED=false

# 资源名称配置（可选）
# --------------------
# 如果不指定，deploy_all.sh 会自动生成随机名称避免冲突
# 如果要更新现有资源，请指定具体名称并使用 update_lambda.sh

# Lambda 函数名称（留空则自动生成）
# LAMBDA_FUNCTION_NAME=BedrockKBMCPProxy

# Lambda IAM 角色名称（留空则自动生成）
# LAMBDA_ROLE_NAME=BedrockKBMCPLambdaRole

# Cognito User Pool 名称（留空则自动生成）
# COGNITO_USER_POOL_NAME=bedrock-kb-mcp-pool

# Cognito 用户名
# COGNITO_USERNAME=admin

# Gateway 配置（可选）
# --------------------
# 如果已有 Gateway，可以指定
# GATEWAY_ID=your-gateway-id
# GATEWAY_NAME=bedrock-kb-mcp-gateway
